
const nav=document.querySelector('.main-nav');document.querySelector('.burger')?.addEventListener('click',()=>nav.classList.toggle('open'));
const modal=document.getElementById('leadModal');document.querySelectorAll('[data-open-modal]').forEach(b=>b.addEventListener('click',()=>modal.classList.add('open')));document.querySelectorAll('[data-close-modal]').forEach(b=>b.addEventListener('click',()=>modal.classList.remove('open')));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});
async function sendLead(form){const result=form.querySelector('.form-result'); result.className='form-result'; result.textContent='Отправляем...'; const data=Object.fromEntries(new FormData(form).entries()); data.page=location.href; try{const res=await fetch('/api/send.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}); const out=await res.json(); if(out.ok){result.classList.add('ok'); result.textContent='Заявка отправлена. Скоро свяжемся.'; form.reset();} else {throw new Error(out.error||'Ошибка отправки');}}catch(e){result.classList.add('err'); result.textContent='Форма готова, но Telegram не настроен. Заполните api/config.php';}}
document.querySelectorAll('.lead-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault(); sendLead(form)}));
